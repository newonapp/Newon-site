/* Business pillar detail pages — editorial B&W studio */
.bp-page {
  --bp-ink: #111;
  --bp-muted: #6e6e73;
  --bp-faint: #a1a1a6;
  --bp-line: #e8e8ea;
  --bp-hover: #f6f6f7;
  --bp-bg: #fff;
  --bp-max: 1120px;
  --bp-ease: cubic-bezier(0.22, 1, 0.36, 1);
  color: var(--bp-ink);
  background: var(--bp-bg);
}

.bp-inner {
  width: min(100% - 2.5rem, var(--bp-max));
  margin-inline: auto;
}

@media (min-width: 768px) {
  .bp-inner {
    width: min(100% - 3.5rem, var(--bp-max));
  }
}

.bp-label {
  margin: 0 0 0.75rem;
  font-size: 0.6875rem;
  font-weight: 650;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: var(--bp-faint);
}

.bp-eyebrow {
  margin: 0 0 1rem;
  font-size: 0.75rem;
  font-weight: 650;
  letter-spacing: 0.14em;
  color: var(--bp-muted);
}

.bp-btn {
  display: inline-flex;
  align-items: center;
  gap: 0.35rem;
  min-height: 2.75rem;
  padding: 0.65rem 1.15rem;
  font-size: 0.9375rem;
  font-weight: 600;
  letter-spacing: -0.01em;
  text-decoration: none;
  border: 1px solid transparent;
  transition: background 0.2s var(--bp-ease), color 0.2s ease, border-color 0.2s ease;
}

.bp-btn--primary {
  background: var(--bp-ink);
  color: #fff;
}

.bp-btn--primary:hover {
  background: #000;
}

.bp-btn--ghost {
  background: transparent;
  color: var(--bp-ink);
  border-color: var(--bp-line);
}

.bp-btn--ghost:hover {
  background: var(--bp-hover);
}

/* Breadcrumb */
.bp-crumb {
  padding: 1.25rem 0 0;
}

.bp-crumb__list {
  display: flex;
  flex-wrap: wrap;
  gap: 0.5rem;
  margin: 0;
  padding: 0;
  list-style: none;
  font-size: 0.8125rem;
  color: var(--bp-muted);
}

.bp-crumb__list li:not(:last-child)::after {
  content: "/";
  margin-left: 0.5rem;
  color: var(--bp-faint);
}

.bp-crumb a {
  color: inherit;
  text-decoration: none;
}

.bp-crumb a:hover {
  color: var(--bp-ink);
}

/* Hero */
.bp-hero {
  padding: 2.5rem 0 3.5rem;
  border-bottom: 1px solid var(--bp-line);
}

.bp-hero__grid {
  display: grid;
  gap: 2.5rem;
  align-items: end;
}

@media (min-width: 900px) {
  .bp-hero__grid {
    grid-template-columns: minmax(0, 1.35fr) minmax(0, 0.65fr);
    gap: 3rem;
  }
}

.bp-hero__title {
  margin: 0 0 1.15rem;
  font-size: clamp(1.85rem, 4.2vw, 2.85rem);
  font-weight: 650;
  letter-spacing: -0.035em;
  line-height: 1.15;
}

.bp-hero__lead {
  margin: 0 0 1.75rem;
  max-width: 36rem;
  font-size: 1.0625rem;
  line-height: 1.65;
  color: var(--bp-muted);
}

.bp-hero__actions {
  display: flex;
  flex-wrap: wrap;
  gap: 0.75rem;
}

.bp-hero__visual {
  justify-self: start;
}

@media (min-width: 900px) {
  .bp-hero__visual {
    justify-self: end;
    padding-bottom: 0.25rem;
  }
}

/* BUILD index */
.bp-index {
  margin: 0;
  padding: 0;
  list-style: none;
  border-top: 1px solid var(--bp-line);
  min-width: 11rem;
}

.bp-index li {
  padding: 0.7rem 0;
  border-bottom: 1px solid var(--bp-line);
  font-size: 0.8125rem;
  font-weight: 600;
  letter-spacing: 0.06em;
  font-variant-numeric: tabular-nums;
  color: var(--bp-ink);
}

/* AUTOMATION flow */
.bp-flow {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  gap: 0.35rem;
  min-width: 10rem;
}

.bp-flow__step {
  font-size: 0.8125rem;
  font-weight: 650;
  letter-spacing: 0.1em;
  padding: 0.55rem 0;
  border-bottom: 1px solid var(--bp-line);
  width: 100%;
  opacity: 0.45;
  transition: opacity 0.35s ease;
}

.bp-flow__step.is-on {
  opacity: 1;
}

.bp-flow__arrow {
  font-size: 0.75rem;
  color: var(--bp-faint);
  padding-left: 0.15rem;
}

/* RESEARCH stack */
.bp-stack {
  display: flex;
  flex-direction: column;
  gap: 0.35rem;
  min-width: 10.5rem;
  border-left: 1px solid var(--bp-line);
  padding-left: 1rem;
}

.bp-stack__item {
  font-size: 0.75rem;
  font-weight: 600;
  letter-spacing: 0.08em;
  color: var(--bp-muted);
}

.bp-stack__arrow {
  color: var(--bp-faint);
  font-size: 0.75rem;
  margin: 0.15rem 0;
}

.bp-stack__result {
  margin-top: 0.25rem;
  font-size: 0.9375rem;
  font-weight: 700;
  letter-spacing: 0.08em;
}

.bp-mark {
  margin: 0;
  font-size: 2.5rem;
  font-weight: 700;
  letter-spacing: -0.04em;
  color: var(--bp-faint);
}

/* Sections */
.bp-sec {
  padding: 3.5rem 0;
  border-bottom: 1px solid var(--bp-line);
}

.bp-sec__head {
  margin-bottom: 1.75rem;
}

.bp-sec__head--row {
  display: flex;
  flex-wrap: wrap;
  align-items: baseline;
  justify-content: space-between;
  gap: 0.75rem 1.5rem;
}

.bp-note {
  margin: 0;
  font-size: 0.8125rem;
  color: var(--bp-muted);
}

/* Services accordion */
.bp-svc-list {
  border-top: 1px solid var(--bp-line);
}

.bp-svc__trigger {
  display: grid;
  grid-template-columns: 2.5rem minmax(0, 1fr) 1.5rem;
  gap: 0.85rem 1rem;
  align-items: start;
  width: 100%;
  margin: 0;
  padding: 1.35rem 0.25rem;
  border: 0;
  border-bottom: 1px solid var(--bp-line);
  background: transparent;
  text-align: left;
  cursor: pointer;
  font: inherit;
  color: inherit;
  transition: background 0.2s var(--bp-ease);
}

.bp-svc__trigger:hover {
  background: var(--bp-hover);
}

.bp-svc__n {
  font-size: 0.75rem;
  font-weight: 650;
  letter-spacing: 0.06em;
  color: var(--bp-faint);
  padding-top: 0.2rem;
  font-variant-numeric: tabular-nums;
}

.bp-svc__main {
  display: flex;
  flex-direction: column;
  gap: 0.35rem;
  min-width: 0;
}

@media (min-width: 720px) {
  .bp-svc__main {
    flex-direction: row;
    align-items: baseline;
    gap: 1.5rem;
  }
}

.bp-svc__title {
  flex: 0 0 auto;
  min-width: 10rem;
  font-size: 1.0625rem;
  font-weight: 650;
  letter-spacing: -0.02em;
}

.bp-svc__summary {
  flex: 1 1 auto;
  font-size: 0.9375rem;
  line-height: 1.5;
  color: var(--bp-muted);
}

.bp-svc__icon::before {
  content: "+";
  font-size: 1.25rem;
  line-height: 1;
  color: var(--bp-faint);
}

.bp-svc.is-open .bp-svc__icon::before {
  content: "−";
}

.bp-svc__panel {
  padding: 0 0 1.5rem 3.35rem;
  border-bottom: 1px solid var(--bp-line);
}

@media (max-width: 719px) {
  .bp-svc__panel {
    padding-left: 0.25rem;
  }
}

.bp-svc__detail {
  display: grid;
  gap: 1rem;
  margin: 0;
  max-width: 40rem;
}

@media (min-width: 720px) {
  .bp-svc__detail {
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1.15rem 1.75rem;
  }
}

.bp-svc__detail dt {
  margin: 0 0 0.25rem;
  font-size: 0.6875rem;
  font-weight: 650;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--bp-faint);
}

.bp-svc__detail dd {
  margin: 0;
  font-size: 0.9375rem;
  line-height: 1.55;
  color: var(--bp-ink);
}

/* Process */
.bp-proc--h {
  display: flex;
  flex-wrap: wrap;
  align-items: flex-start;
  gap: 0.75rem 0.35rem;
}

.bp-proc--v {
  display: flex;
  flex-direction: column;
  gap: 0.35rem;
  max-width: 28rem;
}

.bp-proc__step {
  display: flex;
  flex-direction: column;
  gap: 0.2rem;
  min-width: 6.5rem;
  padding: 0.85rem 0.5rem 0.85rem 0;
  border-top: 1px solid var(--bp-line);
}

.bp-proc--h .bp-proc__step {
  flex: 1 1 6.5rem;
}

.bp-proc__n {
  font-size: 0.6875rem;
  font-weight: 650;
  letter-spacing: 0.08em;
  color: var(--bp-faint);
}

.bp-proc__t {
  font-size: 0.875rem;
  font-weight: 650;
  letter-spacing: -0.01em;
}

.bp-proc__d {
  font-size: 0.8125rem;
  line-height: 1.45;
  color: var(--bp-muted);
}

.bp-proc__conn {
  align-self: center;
  color: var(--bp-faint);
  font-size: 0.75rem;
  padding: 0 0.15rem;
}

.bp-proc__conn--down {
  align-self: flex-start;
  padding-left: 0.15rem;
}

@media (max-width: 899px) {
  .bp-proc--h {
    flex-direction: column;
  }
  .bp-proc--h .bp-proc__conn:not(.bp-proc__conn--down) {
    transform: rotate(90deg);
    align-self: flex-start;
  }
}

/* Use cases */
.bp-usecases__grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 0;
  margin: 0;
  padding: 0;
  list-style: none;
  border-top: 1px solid var(--bp-line);
}

@media (min-width: 720px) {
  .bp-usecases__grid {
    grid-template-columns: repeat(4, minmax(0, 1fr));
  }
}

.bp-usecases__grid li {
  padding: 1.15rem 1rem 1.15rem 0;
  border-bottom: 1px solid var(--bp-line);
  font-size: 0.9375rem;
  font-weight: 500;
  letter-spacing: -0.015em;
}

@media (min-width: 720px) {
  .bp-usecases__grid li:nth-child(4n) {
    padding-right: 0;
  }
}

/* Before / After */
.bp-ba__grid {
  display: grid;
  gap: 1.25rem;
  align-items: start;
}

@media (min-width: 720px) {
  .bp-ba__grid {
    grid-template-columns: 1fr auto 1fr;
    gap: 1.5rem;
  }
}

.bp-ba__k {
  margin: 0 0 0.85rem;
  font-size: 0.6875rem;
  font-weight: 650;
  letter-spacing: 0.1em;
  color: var(--bp-faint);
}

.bp-ba__col ul {
  margin: 0;
  padding: 0;
  list-style: none;
  border-top: 1px solid var(--bp-line);
}

.bp-ba__col li {
  padding: 0.85rem 0;
  border-bottom: 1px solid var(--bp-line);
  font-size: 0.9375rem;
  color: var(--bp-muted);
}

.bp-ba__col--after li {
  color: var(--bp-ink);
  font-weight: 550;
}

.bp-ba__arrow {
  display: none;
  color: var(--bp-faint);
  font-size: 1.25rem;
  padding-top: 2rem;
}

@media (min-width: 720px) {
  .bp-ba__arrow {
    display: block;
  }
}

/* Research doc mock */
.bp-doc {
  border: 1px solid var(--bp-line);
  background: #fafafa;
  max-width: 36rem;
}

.bp-doc__bar {
  display: flex;
  align-items: center;
  gap: 0.4rem;
  padding: 0.65rem 0.85rem;
  border-bottom: 1px solid var(--bp-line);
  background: #fff;
}

.bp-doc__bar span {
  width: 0.45rem;
  height: 0.45rem;
  border-radius: 50%;
  background: var(--bp-line);
}

.bp-doc__bar em {
  margin-left: 0.5rem;
  font-style: normal;
  font-size: 0.625rem;
  letter-spacing: 0.08em;
  color: var(--bp-faint);
}

.bp-doc__body {
  padding: 1.35rem 1.25rem 1.5rem;
  background: #fff;
}

.bp-doc__title {
  margin: 0 0 1.15rem;
  font-size: 1.125rem;
  font-weight: 650;
  letter-spacing: -0.02em;
}

.bp-doc__toc {
  margin: 0 0 1.25rem;
  padding: 0;
  list-style: none;
  border-top: 1px solid var(--bp-line);
}

.bp-doc__toc li {
  display: flex;
  gap: 0.85rem;
  padding: 0.55rem 0;
  border-bottom: 1px solid var(--bp-line);
  font-size: 0.875rem;
}

.bp-doc__toc span {
  color: var(--bp-faint);
  font-variant-numeric: tabular-nums;
  font-size: 0.75rem;
  font-weight: 650;
}

.bp-doc__lines {
  display: flex;
  flex-direction: column;
  gap: 0.45rem;
}

.bp-doc__lines span {
  display: block;
  height: 0.35rem;
  background: var(--bp-line);
  width: 100%;
}

.bp-doc__lines span.is-short {
  width: 55%;
}

/* Launch flow */
.bp-launch__flow {
  margin: 0;
  padding: 0;
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 0;
  max-width: 22rem;
}

.bp-launch__step {
  position: relative;
  padding: 0.85rem 0;
  border-top: 1px solid var(--bp-line);
  outline: none;
}

.bp-launch__t {
  display: block;
  font-size: 1rem;
  font-weight: 650;
  letter-spacing: 0.04em;
}

.bp-launch__tip {
  display: block;
  max-height: 0;
  overflow: hidden;
  opacity: 0;
  font-size: 0.875rem;
  color: var(--bp-muted);
  transition: max-height 0.22s var(--bp-ease), opacity 0.22s ease, margin 0.22s ease;
}

.bp-launch__step:hover .bp-launch__tip,
.bp-launch__step:focus-visible .bp-launch__tip {
  max-height: 3rem;
  opacity: 1;
  margin-top: 0.35rem;
}

.bp-launch__conn {
  display: block;
  color: var(--bp-faint);
  font-size: 0.75rem;
  margin-top: 0.35rem;
}

@media (min-width: 720px) {
  .bp-launch__flow {
    flex-direction: row;
    flex-wrap: wrap;
    max-width: none;
    gap: 0.5rem 0.25rem;
    align-items: flex-start;
  }
  .bp-launch__step {
    flex: 1 1 7rem;
    border-top: 0;
    border-left: 1px solid var(--bp-line);
    padding: 0.25rem 0.75rem 1rem;
  }
  .bp-launch__conn {
    position: absolute;
    right: -0.35rem;
    top: 0.35rem;
    transform: rotate(-90deg);
    margin: 0;
  }
}

/* When to use */
.bp-when__list {
  margin: 0;
  padding: 0;
  list-style: none;
  border-top: 1px solid var(--bp-line);
}

.bp-when__list li {
  display: flex;
  gap: 1rem;
  padding: 1.1rem 0;
  border-bottom: 1px solid var(--bp-line);
  font-size: 1.0625rem;
  letter-spacing: -0.02em;
}

.bp-when__list span {
  font-size: 0.75rem;
  font-weight: 650;
  color: var(--bp-faint);
  padding-top: 0.25rem;
  font-variant-numeric: tabular-nums;
}

/* Pricing */
.bp-pricing__list {
  margin: 0 0 1.25rem;
  padding: 0;
  list-style: none;
  border-top: 1px solid var(--bp-line);
}

.bp-pricing__list li {
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
  gap: 0.5rem 1.5rem;
  padding: 1.15rem 0;
  border-bottom: 1px solid var(--bp-line);
}

.bp-pricing__name {
  font-size: 0.9375rem;
  font-weight: 650;
  letter-spacing: 0.02em;
}

.bp-pricing__price {
  font-size: 0.9375rem;
  font-variant-numeric: tabular-nums;
}

.bp-pricing__note {
  margin: 0 0 1.5rem;
  max-width: 36rem;
  font-size: 0.875rem;
  line-height: 1.55;
  color: var(--bp-muted);
}

.bp-pricing__custom {
  border-top: 1px solid var(--bp-line);
  padding-top: 1.5rem;
  max-width: 36rem;
}

.bp-pricing__custom .bp-pricing__name {
  margin: 0 0 0.5rem;
  font-size: 0.75rem;
  letter-spacing: 0.1em;
  color: var(--bp-faint);
}

.bp-pricing__lead {
  margin: 0 0 0.85rem;
  font-size: clamp(1.5rem, 3vw, 2rem);
  font-weight: 650;
  letter-spacing: -0.03em;
}

.bp-pricing__body {
  margin: 0 0 1.5rem;
  font-size: 0.975rem;
  line-height: 1.6;
  color: var(--bp-muted);
}

/* Other services */
.bp-other__list {
  display: grid;
  gap: 0;
  border-top: 1px solid var(--bp-line);
}

@media (min-width: 720px) {
  .bp-other__list {
    grid-template-columns: repeat(4, minmax(0, 1fr));
  }
}

.bp-other__item {
  display: grid;
  grid-template-columns: auto 1fr auto;
  gap: 0.65rem;
  align-items: center;
  padding: 1.15rem 0.85rem 1.15rem 0;
  border-bottom: 1px solid var(--bp-line);
  text-decoration: none;
  color: inherit;
  transition: background 0.2s var(--bp-ease);
}

.bp-other__item:hover {
  background: var(--bp-hover);
}

.bp-other__item.is-active {
  background: var(--bp-hover);
  pointer-events: none;
}

.bp-other__n {
  font-size: 0.6875rem;
  font-weight: 650;
  color: var(--bp-faint);
}

.bp-other__t {
  font-size: 0.9375rem;
  font-weight: 650;
  letter-spacing: -0.01em;
}

.bp-other__arrow {
  color: var(--bp-faint);
  transition: transform 0.2s var(--bp-ease);
}

.bp-other__item:hover .bp-other__arrow {
  transform: translateX(4px);
  color: var(--bp-ink);
}

.bp-other__item.is-active .bp-other__arrow {
  opacity: 0;
}

/* FAQ */
.bp-faq__list {
  border-top: 1px solid var(--bp-line);
}

.bp-faq__item {
  border-bottom: 1px solid var(--bp-line);
  padding: 0;
}

.bp-faq__item summary {
  cursor: pointer;
  padding: 1.15rem 1.5rem 1.15rem 0;
  font-size: 1rem;
  font-weight: 600;
  letter-spacing: -0.015em;
  list-style: none;
}

.bp-faq__item summary::-webkit-details-marker {
  display: none;
}

.bp-faq__item p {
  margin: 0 0 1.25rem;
  max-width: 40rem;
  font-size: 0.9375rem;
  line-height: 1.65;
  color: var(--bp-muted);
}

/* Final CTA */
.bp-cta {
  padding: 4.5rem 0 5rem;
  background: var(--bp-ink);
  color: #fff;
}

.bp-cta .bp-label {
  color: rgba(255, 255, 255, 0.45);
}

.bp-cta__title {
  margin: 0 0 1rem;
  max-width: 28rem;
  font-size: clamp(1.65rem, 3.5vw, 2.35rem);
  font-weight: 650;
  letter-spacing: -0.035em;
  line-height: 1.2;
}

.bp-cta__lead {
  margin: 0 0 1.75rem;
  max-width: 32rem;
  font-size: 1.025rem;
  line-height: 1.6;
  color: rgba(255, 255, 255, 0.65);
}

.bp-cta .bp-btn--primary {
  background: #fff;
  color: var(--bp-ink);
}

.bp-cta .bp-btn--primary:hover {
  background: #f0f0f0;
}

/* Reveal */
[data-bp-reveal] {
  opacity: 0;
  transform: translateY(12px);
  transition: opacity 0.55s var(--bp-ease), transform 0.55s var(--bp-ease);
}

[data-bp-reveal].is-in {
  opacity: 1;
  transform: none;
}

@media (prefers-reduced-motion: reduce) {
  [data-bp-reveal],
  .bp-flow__step,
  .bp-launch__tip,
  .bp-svc__trigger,
  .bp-other__item {
    transition: none !important;
  }
  [data-bp-reveal] {
    opacity: 1;
    transform: none;
  }
}

/* Layout accents per pillar */
.bp--build .bp-hero {
  padding-bottom: 4rem;
}

.bp--automation .bp-usecases {
  background: #fafafa;
}

.bp--research .bp-output {
  background: #fafafa;
}

.bp--solutions .bp-when__list li {
  font-size: 1.125rem;
}
